{
  "name": "skiftschema-scraper",
  "version": "1.0.0",
  "description": "Automated scraper for skiftschema.se shift schedules",
  "main": "scripts/scrape-upload-cursor.cjs",
  "scripts": {
    "scrape-teams": "node scrape-skiftschema.js",
    "scrape-upload": "node scripts/scrape-upload-cursor.cjs",
    "start": "npm run scrape-teams && npm run scrape-upload",
    "test": "node -e \"console.log('✅ Dependencies loaded successfully')\" && node -p \"require('./scripts/scrape-upload-cursor.cjs').TEAMS.length + ' teams configured'\""
  },
  "keywords": [
    "scraper",
    "puppeteer", 
    "supabase",
    "skiftschema",
    "shift-schedule"
  ],
  "author": "Cursor Agent",
  "license": "MIT",
  "dependencies": {
    "puppeteer": "^21.5.2",
    "@supabase/supabase-js": "^2.38.4",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
