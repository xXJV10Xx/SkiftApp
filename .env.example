# ===========================================
# GOOGLE OAUTH KONFIGURATION
# ===========================================
# Gå till https://console.developers.google.com/
# Skapa nytt projekt eller välj befintligt
# Aktivera Google Calendar API och Google+ API
# Skapa OAuth 2.0 credentials

# Google OAuth Client IDs (olika för varje plattform)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# För mobilappen (Expo)
EXPO_PUBLIC_GOOGLE_EXPO_CLIENT_ID=your_expo_client_id_here
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=your_ios_client_id_here
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=your_android_client_id_here
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your_web_client_id_here

# ===========================================
# BACKEND KONFIGURATION
# ===========================================
PORT=3000
BACKEND_URL=http://localhost:3000
FRONTEND_URL=http://localhost:3001

# För produktion:
# BACKEND_URL=https://your-backend-domain.com
# FRONTEND_URL=https://your-frontend-domain.com

# ===========================================
# SUPABASE KONFIGURATION
# ===========================================
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ===========================================
# MOBILAPP KONFIGURATION
# ===========================================
EXPO_PUBLIC_API_URL=http://localhost:3000
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# ===========================================
# WEBBAPP KONFIGURATION
# ===========================================
REACT_APP_API_URL=http://localhost:3000

# ===========================================
# OAUTH REDIRECT URLS (för Google Console)
# ===========================================
# Lägg till dessa i Google Cloud Console:
# 
# Web:
# - http://localhost:3000/auth/google/callback (utveckling)
# - https://your-backend-domain.com/auth/google/callback (produktion)
# 
# Mobile (Expo):
# - https://auth.expo.io/@your-username/your-app-slug
# - exp://localhost:19000/--/ (utveckling)
# 
# iOS:
# - com.skiftappen.app:/oauth (från app.json bundleIdentifier)
# 
# Android:
# - com.skiftappen.app:/oauth (från app.json package)

# ===========================================
# DATABAS TABELLER SOM BEHÖVS
# ===========================================
# Kör dessa SQL-kommandon i Supabase:
#
# CREATE TABLE users (
#   id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
#   email VARCHAR UNIQUE NOT NULL,
#   full_name VARCHAR,
#   avatar_url VARCHAR,
#   google_id VARCHAR UNIQUE,
#   created_at TIMESTAMP DEFAULT NOW(),
#   updated_at TIMESTAMP DEFAULT NOW()
# );
#
# CREATE TABLE google_tokens (
#   id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
#   user_id UUID REFERENCES users(id) ON DELETE CASCADE,
#   access_token TEXT NOT NULL,
#   refresh_token TEXT,
#   token_type VARCHAR DEFAULT 'Bearer',
#   scope TEXT,
#   expires_at TIMESTAMP,
#   created_at TIMESTAMP DEFAULT NOW(),
#   updated_at TIMESTAMP DEFAULT NOW(),
#   UNIQUE(user_id)
# );